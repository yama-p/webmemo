<p>お世話になります。 <%= @resource.email %>!</p>

<p>パスワードを変更用の通知です。以下のリンクへアクセスして変更を完了してください。</p>

<p><%= link_to 'パスワード変更を完了する', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>心当たりのない場合は無視してください。</p>
<p>上記リンクにアクセスし変更を完了するまで、パスワードは変更されません。</p>
